# Lezione 17
 Ok, vediamo qualche esempio, qualche ulteriore esercizio, abbiamo già visti diversi, però facciamo qualche altro e poi andiamo un po' avanti, va bene?
 a un certo punto avevamo detto mostrare che ACFG tale che G è una CFG
 a L di G ok? L di G ok? questo avevamo detto a un certo punto, vediamo un attimo come si può ragionare su questo diciamo qui ovviamente qual è la difficoltà che questo linguaggio è facilmente riconoscibile perché per capire se W è una stringa che in generale
 a partire dalla grammatica posso provare a produrla utilizzando le regole della grammatica ok? però il problema è che queste regole possono essere ripetute in maniera arbitraria quindi in generale il numero di modi di derivare una stringa potrebbe essere infinito ci potrebbero essere infiniti modi di derivare una stringa ok? quindi provarli tutti mi direbbe soltanto
 è riconoscibile ok? se uno esiste prima o poi lo trovo però se non esiste potrei andare avanti all'infinito si? ok? quindi il problema ci possono essere infinite di un'unità ci possono essere infinite di un'unità
 ok? quindi in generale qui ci aiuta una proprietà che poi è un semplice esercizio diciamo il fatto che se G è in normal form forma normale allora
 ogni W appartenente a L e G tale che cardinalità di W è uguale a N sono necessari sono richiesti 2N-1 passi per ogni derivazione per ogni sua derivazione
 ok? 2 elevato è no la lunghezza del cammino è 10 ok? va bene? ok questo diciamo si dimostra sufficientemente per l'induzione si dimostra per l'induzione
 no? chiaro o vero? se N è uguale a 1 allora se N è uguale a 1 allora c'è la regola
 S è W giusto? ok è un solo passo e e e infatti e infatti diciamo 2 per 1 è 1 per 1 ok? cioè sarebbe la lunghezza il numero di passi un passo ok? si?
 va bene? se se invece se N è maggiore o uguale a 2 assumo 0 per lunghezze per lunghezze minori di N certamente minori di N e sia
 ok? quindi siccome è più lunga di 1 no? per forza sicuramente la situazione sarà S produce BC
 produce star W no? perché questa è in forma normale quindi la prima la prima derivazione deve essere per forza del tipo S BC no? ok? quindi questa W adesso la puoi pensare così che W genera W1 e C genera W2 no? ok? dove e tale che diciamo la cardinalità di W1 è uguale a I che è minore di N
 e la cardinalità di W2 è uguale a N meno I pure questa meno di N ok? quindi per induzione per ipotesi induttiva per ipotesi induttiva abbiamo che numero di passi numero di passi
 per W no? ok? qual è il numero di passi per W? è il numero di passi è un passo uno il primo passo per andare da S a BC si? più il numero di passi per generare W1 più il numero di passi per generare W2 ok? ma siccome W1 è minore di N W2 è minore di N questo sarà 2 volte I meno 1 più 2 volte N meno I meno 1
 giusto? ok? e quindi questo è 2I più 2N meno 2I meno 2N ok?
 sicuramente sicuramente 2 punti si ok? no no con la formula cancella puoi spiegare no siccome è in forma normale no? ok? e la lunghezza della stringa è più grande di 1 ok? vuol dire che perché siamo in forma normale no? quindi le uniche regole che esistono nella grammatica sono della forma S produce BC oppure S produce terminale ma produce terminale non può essere perché sennò sarebbe lunga 1 deve essere lunga 2
 quindi per forza la prima regola è della forma S produce BC dove B e C sono variabili questa è la definizione della forma normale poi dopo BC produrrà W tramite le regole della grammatica infatti c'è lo star no? c'è lo star
 che mi permette di decidere quel linguaggio perché adesso so che se io ho una qualsiasi stringa W questa ha dimensione N non devo provare tutte le derivazioni possibili di qualsiasi lunghezza ma soltanto tutte quelle di lunghezza 2N meno 1 queste sono finite e quindi posso decidere il linguaggio ok? quindi ho deciso adesso un ATM prova tutte le derivazioni
 di lunghezza 2N meno 1 ok? quindi è difficile ok? tutto qua
 domande? si e poi la grammatica se la grammatica fosse abituale sì sì certo
 però diciamo l'assunzione che cioè il fatto che la grammatica sia o non sia in forma normale in realtà non cambia le stringhe che stanno dentro a quei linguaggi cioè nel senso che senza perdere generalità tutte le grammatiche contestuali possono essere in forma normale quindi questa è una cosa un po' cioè però sì cioè se se il linguaggio fosse una grammatica articolare deve prima trasformare in forma normale ok?
 però diciamo il punto è che ovviamente se G è una grammatica contestuale no? per ogni G esiste un G' che è equivalente a G e che come scusi? certo sì in generale sì quindi la macchina di Turing deve convertirla in generale perché?
 il linguaggio è definito per grammaticamente speggere grazie a tutti cominciamo a far caso va bene va bene 5 minuti e spegniamo mi sembra che ci siamo avviatamente oh va bene un'altra cosa simile pure una cosa che abbiamo visto invece è
 che anche questo linguaggio è decidibile questo è il linguaggio è E, G, F, G G appartenente a C, F, G e L, D, C
 ok cioè anche qui diciamo non posso provare a generare tutte le stringhe perché possono essere G, G, F e non posso provare a generare tutte le stringhe
 perché possono essere G, G, F, G ok però posso fare una cosa simile posso fare una cosa simile a quella che abbiamo fatto pure per per decidere se un DFA accetta almeno una stringa cioè quando è che un DFA accettava almeno una stringa se se vi ricordate se c'era un cammino cioè posso vedere il DFA come un grafo no? e ci deve essere un cammino
 c'è una domanda? sì perché il linguaggio può essere infinito no? cioè siccome il linguaggio in generale è un sottensibile di sigma star può essere infinito e quindi lei non è che può provarle tutte a vedere se sperando che magari ne trova una che genera questo infinito non si fermerebbe sarebbe un decisore deve essere un decisore non può andare lui ok
 e quindi l'idea è un'idea è quella di usare tipo un algoritmo di marcatura come abbiamo fatto pure per il caso del DFA cioè quando è che è vero che un insieme di terminali può essere generato a partire da S no? cioè questo significa che una stringa a partire dalla grammatica significa che c'è un insieme di terminali che può essere generato a partire da S quindi quello che farò
 sarà vado a marcare prima i terminali no? e poi marco tutte le variabili che possono generare quei terminali e poi marco tutte le variabili che possono generare tutte quelle variabili fino a che non mi fermo perché ho marcato tutto quello che potevo marcare se non ho marcato S non c'è neanche niente sì? ok quindi questa è l'idea usiamo invece
 un algoritmo di marcatura ok cioè su input G marca tutti i terminali poi ripeti
 fino a che nessuna nuova variabile è marcata e marca le variabili a tale che
 G ammette la regola A 1 su K e 1 su K ok cioè marco tutte le variabili
 e poi marco tutto quello che può generare una cosa che già ha marcato sì? accetta se e solo se S non è ok
 facciamo tutte le slane ancora
 allora
 è una U o una V? io l'ho detto che era come una U è una U è una U è una U è una U
 professore scusi quella è una V? è la regola penultima riga ok
 ok no perché l'esercizio sono riduzioni vediamo L L macchina di Turing M descrizione di Tm
 e come suona? credo tale che M accetta
 tutte e sole le stringhe di lunghezza dispari ok? dispari in 0,1 star ok? un po' verboso però M accetta tutte e sole le stringhe di lunghezza dispari in 0,1 star L è un linguaggio
 non è riduzione ok? si? ok come facciamo? per riduzione A di Tm è uguale a M di L ovvero
 definisco definisco F sigma star sigma star tale che per ogni input M W appartiene a A di Tm se e solo se F di M W appartiene a A di Tm
 si? riduzione riduzione mapping reduction ok? se questo è vero sappiamo che siccome A di Tm è indefinibile L è indefinita si? e quindi devo definire F ok F calcolabile ok? quindi questa F deve mappare istanze sì, istanze sì istanze no, istanze no quindi qual è l'istruzione che devo seguire? devo costruire la F in modo che quando
 quindi la F mi deve dare in output una macchina di Turing la descrizione di una macchina di Turing ok? M' chiamiamo ok? calcolabile restituisce M' l'ha definita ok? e qual è la nostra intuizione che M' come deve essere fatta? deve essere fatta così che se M W appartiene a A di Tm allora M'
 appartiene a L ok? ovvero M' è una Tm che accetta tutte e sole le stringhe di lunghezza disparate ok? e viceversa se invece F se invece M di W non appartiene alla Tm cioè se M va in loop o rifiuta W allora deve succedere il contrario cioè il linguaggio di M' non deve essere il linguaggio di tutte le stringhe zero non star di lunghezza disparate
 ok? quindi la macchina di Turing che la macchina M' in questo caso direttamente è su input x se cardinella di x è pari è rifiuta altrimenti
 e esegui M su W e e accetta x se e solo se M accetta x ok? ok?
 quindi vediamo se M se M W appartiene ad A di Tm
 M di W è uguale ad x set ok? allora in questo caso in questo caso qual è il linguaggio di M' perché? L no L è sbagliato non è L il linguaggio di M' il linguaggio di M' è un insieme di stringhe in zero non star ma non sono codifiche di macchina di Turing
 si i due punti che ho scritto qui mi fa la F mi fa F beh questa che sto scrivendo qui è la descrizione di M' F è la macchina di Turing che dato una descrizione di M e di W dà in output la descrizione di M' quindi questa è la descrizione di M' si questa è la descrizione di M' F è la funzione che prende l'input
 M W e restituisce in output la descrizione di M' questa è la descrizione di M' esatto si esatto F è calcolabile devi dire che è calcolabile ma questo è vero perché vedi su input cioè per ispezione no? cioè questa F esiste una macchina di Turing che calcola quella funzione no?
 deve eseguire M su W e cioè per per calcolare F tutto quello che deve fare è rifiutare dell'input a lunghezza quindi deve contare la lunghezza dell'input e se è calcolabile restituirà sempre M' restituirà sempre M' cioè calcolabile significa che esiste una macchina di Turing che prende quell'input no? e ridà e scrive sul nastro
 alla fine l'output della funzione per ogni possibile input sempre si si ok cioè attenzione che la macchina di Turing che calcola F ok cioè non è che esegue M su W no? contiene le istruzioni per eseguire M su W no? cioè ok
 cioè nel senso che se la cardinalità di X è pari allora dirà devi eseguire M su W cioè la sua delta devi definire la delta di M' cioè quello che fa M' dopo che ha controllato che la cardinalità di X è pari no? ok cioè questa è importante no? cioè la macchina di Turing che definisce M' non esegue mai M su W mai contiene solo le istruzioni per farlo
 ok? perché dà in output la quantifica di una macchina di Turing è chiaro? è chiaro? si? è chiaro? però se invece vuoi vuoi invece adesso no? nella prova di correttezza ragioniamo su quello che è il linguaggio di M' ok? cioè il linguaggio di M' sono
 tutte le stringhe accettate da M' sì? ha senso? ok quindi rivedi il linguaggio a cui facciamo l'edizione in questo caso ATM accetta sempre l'edizione in generale ecco cioè la macchina di Turing che riconosce il nostro linguaggio a cui vogliamo fare l'edizione nel nostro caso ATM
 accetta sempre aspetta in quale caso scusi? in questo caso qua? sì in questa direzione qua chi è che accetta sempre? M' M' accetta sempre cioè M' accetta sempre che significa sempre perché stiamo fissando una particolare input ok? cioè quindi stiamo fissando M' e stiamo fissando W quindi M' accetta quel W cioè non è sempre cioè non capisco il significato di sempre perché non c'è sempre cioè è un input fissato
 nel senso abbiamo visto che non esiste per ATM abbiamo visto che non esiste sì ma questo se lo può ascoltare adesso? sì esatto quindi diciamo stiamo supponendo che funzioni che ATM esista e funzioni e decida no non sto facendo questo cioè io in questo nella mia tecnica di risoluzione ok? mi sto mi sto ignorando il fatto che abbiamo
 già dimostrato che ADTM non è decidibile quindi se adesso se adesso quel linguaggio fosse decidibile allora ci sarebbe un decisore per ADTM ok? questo non lo sto facendo non è quello che sto facendo adesso perché l'ho già dimostrato ok? e io sto so che un modo generale per far vedere che un linguaggio non è decidibile è ridurlo ad ADTM ok? quindi in questa risoluzione io sto facendo solo la riduzione tra ADTM e il linguaggio
 non sto facendo la contraddizione che se poi quel linguaggio fosse decidibile allora perché quello è un fatto generale che so che è sempre vero cioè quindi in altre parole all'esame io farei così cioè ok direi ok allora devo dimostrare che questo linguaggio non è decidibile ok? abbiamo già dimostrato che ADTM non è decidibile cioè siccome questo è un esercizio non è una domanda di teoria non è come dovete dimostrare che ADTM non è decidibile ok? questo lo potete dare per buono ADTM non è decidibile ok? poi
 posso dire ok? c'è un teorema generale che dice che se appunto esiste una riduzione da ADTM ad L e ADTM non è decidibile allora L non è decidibile ok? adesso la dimostrazione di questa cosa so due righe ok? se la volete fa la fate se non la volete fa non la fate non vi punirò perché non l'avete fatta ok? cioè questa è la mia filosofia perché ripeto non è una domanda di teoria ok? però vi scriverete abbiamo dimostrato in classe
 che va bene mi sta bene cioè perché magari non c'è tempo cioè comunque la dimostrazione di questa cosa sarebbero due righe ok? se uno la vuole fa la fa se non la fa non fa niente e a questo punto dati questi presupposti tutto quello che devi fare è soltanto definire F base solo F ok? quindi stiamo definendo F ok? nel definire F dobbiamo dire c'è una macchina di Turing che calcola F prende un input e dà un output ok? e questo è la macchina di Turing
 questa è diciamo la descrizione della macchina di Turing che dà un output la macchina di Turing che la calcola è implicita però diciamo per descrizione è banale ok? si? a questo punto devo far vedere soltanto che mappa tutte le istanze si nelle istanze si tutte le istanze no nelle istanze no questo è tutto quello che devo fare basta non devo fare altro cioè dell'indecidibilità di ADTM me ne frego
 e posso sfruttare ADM per fare la mapping cioè nel senso sto facendo ok? adesso sto facendo la mappa ok? cioè la mappa l'ho definita si? siamo d'accordo? ho definito F si ok che dà M' che dà M' esatto adesso devo dimostrare che questa è una mapping reduction ovvero che mappa tutte le istanze si nelle istanze si tutte le istanze no nelle istanze no quindi tutte le mappe accettate da MW in mappa
 tutti gli input del linguaggio A in output della funzione nel linguaggio B si? il linguaggio A è ADTM quindi vuol dire che siccome devo dimostrare che tutte le istanze si vengono mappate nelle istanze si come faccio? prendo una qualsiasi istanza si qualsiasi qualsiasi significa fisso M e fisso W quindi M è una macchina di Turing fissata W è una macchina di Turing è una una stringa fissata e sta dentro il linguaggio ADTM ovvero
 L accetta quella stringa solo quella quella particolare stringa fissata ok? si? ci siamo fino qua? perché deve essere una mapping reduction da ADTM ad L cioè quindi no perché perché vedi L è un linguaggio che è fatto
 soltanto da descrizioni di macchine Turing non pure da stringa se no sarebbe una mapping reduction valida perché non mappa nell'insieme corretto ok e quindi diciamo ok M accetta tutto quindi qual è il linguaggio di M' cioè quali sono le stringhe che sono accettate da M' tutte quelle di lunghezza? perché? si
 se M è pari la rifiuta ok? cioè quindi prende un X qualsiasi a questo punto se X è pari la rifiuta ok? quindi tutte le X pari sono rifiutate per quanto riguarda le X dispari le accetterà tutte perché lancia M su W ma noi M W accetta quindi e lei accetta X se solo se M accetta W quindi accetta tutte le X successive delle che non l'hanno che l'hanno portata in questo ramo di conclusione si? ok
 ora quindi L di M' è uguale a tutte le stringhe tutte le stringhe X in 0,1 stagio tale che X a lunghezza dispari
 e quindi ovvero M' appartiene a sì? ok cioè quindi questo sì giusto? quindi abbiamo mappato tutte le istanze sì nelle istanze sì perché una volta le istanze sì viene mappata in un'istanza sì
 ok l'altro caso è quello in cui se M W non appartiene alla D di M ok? allora M W rifiuta M W diciamo appartiene a reject infinito ok?
 sì? quindi quindi ma allora e L di M' in questo caso qui è L di M' questo è? W e M' non appartiene
 a R sì? sì? perché è vuoto è vuoto perché perché su input X se X ha lunghezza pari rifiuta se X ha lunghezza quindi tutte quelle pari le rifiuta tutte quelle dispari le accetterebbe se solo se M accetta W ma M rifiuta tutti W o va in loop quindi non accetterebbe
 nessuno quindi non accetta nessuno quindi non accetta niente sì? bene? va bene? va bene? un altro? ne diamo un altro? sì sì un attimo solo prof sì qua ok
 grazie L è uguale a M L è uguale a M o rifiuta o non accetta N è uguale a M un attimo solo prof sì e
 L di M è uguale 0 a N 1 a N 0 a N N è uguale ok? ok quindi L è quantità di linguaggio di tutte le descrizioni
 di macchine di Turing il cui linguaggio è 0 a N 1 a N 0 a N per un qualche anno sì? no perché la prima parentesi graffa chiude il linguaggio L di M e la seconda chiude invece il linguaggio L che è stato definito ah io capisco sì? sì sì ok bene ok
 anche qui mostro A di M è uguale a M di L ok? e costruisco F sigma star in sigma star calcolabile
 tale che M W appartiene ad A di T M se e solo se F di M W appartiene a L ok? va bene? e F calcolabile ok? questo è il codice
 per M' uguale F di M' ok? quindi la macchina di Turing che calcola F è la macchina di Turing che dato in input M W su un nastro di input deve terminare con output alla descrizione di M' dove M' è la seguente macchina di Turing ok? su input X
 X simula M su W ok? a questo punto se X se M di W è uguale a X' ok?
 e X è uguale a 0 alla N 1 alla N 0 alla N sì? per N maggiore o uguale di 0 allora M' di X è uguale ad X ok?
 se invece X diverso da 0 alla N 1 alla N 0 alla N M' di X è uguale ad R' ok? se se invece M di W è uguale ad R' M' di X è uguale ad R'
 ok? la logica dovrebbe essere sempre la stessa ok? cioè quindi cerco di ricostruire questa macchina M' tale che appunto se M accetta W L di M' è di quella forma e altrimenti no vediamo se ho fatto bene ok? è chiara la descrizione? sì
 se X è uguale a 0 alla N 1 alla N 0 alla N o che prima si chiama N oppure potrebbe essere lo stesso cioè in giro di M' fa niente è importante cioè niente di tutto quello che io sto facendo è completamente arbitrario e potrebbe essere che c'è un modo diverso di definire M' e potrebbe essere corretto o no e quindi no non è inerente però se vuole possiamo vedere dopo ok? se vuole proporre un'altra un'altra mapping reduction la possiamo analizzare subito dopo beh cioè diciamo che l'ordine in cui
 quale cosa è importante per definire qual è il linguaggio di M' cioè potrebbero essere due valenti o potrebbero non esserlo e questo quindi potrebbe essere giusto o sbagliato ma se vuole se ha una proposta la possiamo analizzare ok? però finiamo questa così non ci confondiamo ok? cioè così ne abbiamo definita una però ripeto cioè ok? se io sto facendo una mapping reduction da un linguaggio L ad A di D non è che ce ne sta una sola ok? quindi potrebbe essere che lei ne ha trovato un altro modo e quello va bene uguale se è corretto è corretto però
 la cosa importante è che tutte le istanze sì sono qua le istanze sì tutte le istanze no sì? ok? cioè questo è un modo diverso il mio modo è corretto perché il mio modo è corretto? perché se appunto M' appartiene ad A di M allora M' è uguale ad AXE se M' è uguale ad AXE cioè che significa? che quali input X accetto?
 cioè quindi vuol dire che la macchina M' nell'ultimo branch non ce l'ha mai perché M' è uguale ad AXE non ce l'ha mai sì? su ogni input X la macchina M' sta solo nel primo branch diciamo nel primo IF no? nel primo IF che fa? accetta X se solo si ha la forma 0 a la N ma la N è 0 a la N sì? quindi accetta tutti e soli l'input X della forma 0 a la N ma la N è 0 a la N sì? in questo caso L di M'
 è uguale a 0 a la N giusto? tutti e soli no? quindi M' appartiene a L va bene? l'altra direzione se M di W
 allora L e M di W appartiene diciamo a REJECT loop e in questo caso che succede? che che succede in questo caso?
 in questo caso L di M' è uguale a 0 a la N puoi ok? va bene? c'è una proposta? ok vediamo definisca la sua proposta ok? la proposta tematica aspetta
 vedi come l'hai detta ok? proposta alternativa ok? su input X eh? sì sì sì sì sì sì
 altrimenti simula M su X o su W? M su W M su W sì accetta cosa? X M' di uguale a Z se solo se
 M di W uguale a Z sì? è questa? ok e si ok quindi vediamo supponiamo A M W in A di M ok? quindi M di W è uguale ad X Z
 ok? in questo caso eh se X è diverso a 0 a la N 0 a la N allora lo rifiuta e quindi giusto L di M' è uguale a 0 a la N 0 a la N 0 a la N sì? questo è giusto?
 questo va bene se M W uguale a Z L in questo caso se X è diverso da quello lo rifiuta altrimenti il linguaggio è buono vi sembra giusto? corretto allora
 quindi va bene ok va bene? eh sì e facciamo un altro esempio facciamo un altro esempio qua dietro
 un altro esempio qua dietro un altro esempio qua dietro un altro esempio qua dietro un altro esempio qua dietro un altro esempio qua dietro un altro esempio qua dietro ci ha un ultimo esercizio però non accetta quindi questa cosa è in falso un esercizio qua che vediamo?
 ok ecco in questo caso L di M' sarebbe vuoto non ho ancora capito quello a quel punto là ok e allora siamo nel caso in cui M va in loop o rifiuta W quindi M' su input X simula M su W il primo ramo se M' accetta non c'entra mai non succede mai cioè questo M' in questo caso non esegue mai questo ramo di computazione esegue solo quello
 ok quello dice che eh sì quello dice che se M di W rifiutasse allora M' rifiuterebbe X sì quindi vuol dire che eh quando lei lancia M di M' di X ok va soltanto in questo ramo qui giusto? solo nell'ultimo ramo ok quali sono gli input che lei accetta?
 nessuno perché vedi cioè lei se M di W rifiuta lei rifiuta X ok se M di W va in loop lei va in loop ah perché proprio di base cioè si basa sul fatto che M cioè ha reject ogni volta quindi per reject o loop? è loop o loop non possiamo dire reject reject o loop e per avere quella condizione dobbiamo fare che M' non accetta proprio di base cioè non ha un linguaggio in questo caso M' è vuoto è vuoto il linguaggio perché? perché appunto se rifiuta lei rifiuta
 X quello rifiuta W lei rifiuta X per ogni X sì ok se invece andasse in loop lei andrebbe sempre in loop e quindi il suo linguaggio è comunque vuoto ok ok dimostrare questo che se A è tuning riconoscibile
 non so come ci capire e A M' uguale M di non A allora A è decisa ok se A è tuning riconoscibile
 e A M' uguale M di non A allora A è decidibile ok ok idea diciamo idea di base qua che se A è
 tuning riconoscibile e co-turing riconoscibile co-turing riconoscibile allora A è decidibile giusto? mi ricordate? abbiamo dimostrato che se A è tuning riconoscibile e anche co-turing cioè non A
 ha pure co-turing riconoscibile allora A è decidibile quindi siccome A è co-turing riconoscibile devo solo dimostrare che non A è co-turing riconoscibile ok? ok ma adesso ma inoltre A M' uguale M di non A implica che non A M' uguale M
 di A ricordate questa cosa? con la stessa mapping reduction F pure questo l'abbiamo dimostrato no? ok e adesso sappiamo quindi che non A è minore uguale M di A si? ma siccome A è tuning riconoscibile non A è co-turing riconoscibile
 si? siccome A è tuning riconoscibile allora non A pure ok? basta finisce qua ok? cioè non A è tuning riconoscibile ovvero A è co-turing riconoscibile
 ma pure tuning riconoscibile A è decidibile ok? va bene? va bene? domande? va bene qui diciamo esercizi di calcolabilità più o meno su questo lì ok? abbiamo visto i tanti no?
 va bene? ok allora cominciamo a parlare un po' di complessità ok? giusto? le prime dimensioni e poi ci fermiamo ok? entriamo nell'ultima parte del corso va bene? ok? giusto? riscatano scusa mi vedo agitato
 no ok nell'ultima parte del corso nell'ultima parte del corso ci chiediamo ci interroghiamo adesso ci interroghiamo sull'efficienza
 ok? nella risoluzione dei problemi di alcuni problemi nella risoluzione di problemi ok? decisione risoluzione di problemi
 per noi è decisione di linguaggi ok? come abbiamo visto nel corso e infatti abbiamo visto che per tanti problemi cioè per tanti linguaggi sappiamo fare delle macchine turing decisori per altri linguaggi è impossibile fare macchine turing decisori né tanto meno riconoscitori giusto? ok? quindi comunque ci saranno linguaggi già sappiamo che ci saranno
 sono linguaggi che sono irrisolvibili problemi irrisolvibili cioè linguaggi indecidibili senza neanche aver messo in mezzo le risorse cioè senza aver detto cioè deve essere veloce o lento o qualcosa adesso invece vogliamo capire qual è il peso delle risorse ok? cioè vogliamo cominciare a parlare di risorse ok? efficienza quindi in termini di risorse
 ok? e ovviamente adesso le risorse cambiano a seconda di qual è il modello di computazione che usiamo però per noi diciamo le principali saranno spazio e tempo tempo e spazio ok? spazio tipo memoria diciamo ok? e vedremo che spazio e tempo sono molto diverse tra loro
 ok? va bene e poi diciamo ovviamente ce ne sono tante altre ok? di risorse per cui possiamo misurare l'efficienza però di cui non parleremo nel corso cioè noi in questo corso ci soffocalizzeremo solo su questo ok? guarderemo questo però in realtà diciamo ce ne sono tante altre no? cioè tipo random questo è un'altra grandissima
 caratteristica cioè quanta randomicità di quanta randomicità gli algoritmi hanno hanno bisogno e se la randomicità fa una differenza nella risoluzione dei problemi o no ok? questo è un altro esempio se ci sono modelli di calcolo tipo per algoritmi parallelizzabili potrebbe essere non so il numero di processori ok? per calcolo parallelo
 ok? ma per parlare di questo dovremmo cambiare il modello di computazione ok? quindi non non vedremo questo non parleremo neanche di randomness sebbene la randomness invece è una cosa che si può fare pure con le con le macchine di Turing ovviamente non lo faremo però non parleremo di randomness va bene esempio diciamo
 esempio esempio e il linguaggio path ok? path è un linguaggio il cui input scriviamo a cavo sono codifiche è una codifica di un grafo con due nostri
 nodi ok? è chiaro che esiste il cammino da S a T nel grafo ok? G grafo S T grafo ok? cioè quindi ti do un grafo ti do un due nodi del grafo esiste un cammino da questi due nodi nel grafo
 ok? sì e se esiste lo posso stabilire con quanto tempo in quanto spazio ok? cosa di questo tipo quanta memoria mi serve per deciderlo? va bene? e va bene? ok quanto tempo serve? quanto tempo quanto spazio
 va bene? ok la risposta a queste domande fa parte diciamo della teoria della complessità della complessità ok?
 che è un po' diciamo la parte moderna dell'informatica teorica cioè tutto quello che è successo dopo Turing ok? Turing calcolabilità complessità successiva ok? e diciamo è anche la parte dell'informatica teorica con più problemi aperti cioè un sacco di cose non le sappiamo ok? cioè quindi tantissimi tantissimi problemi
 aperti ok? e vedremo beh parleremo un po' di alcuni di questi ma adesso diciamo come motivazione no? ve li dico quali sono ok? cioè ovviamente i simboli che scriverò non avranno significato però ve li dirò anche a parole no? ok? cioè e quindi il primo problema il problema aperto più grande è questo no? cioè è P
 uguale a NP ok? ovvero trovare una soluzione è equivalente è trovare una soluzione trovare una soluzione è tanto veloce
 quanto verificarne una ok? cioè vedremo che per alcune classi di problemi quindi diciamo in generale vogliamo capire quanto è difficile trovare una soluzione ok? quanto è difficile ok? come vedremo ci saranno classi di problemi per cui è facile trovare una soluzione ok? facile significa facile che significa efficiente in termini di risorse ok? e in particolare P
 sarà infatti l'insieme di tutti i problemi per cui è una soluzione che si può trovare in tempo breve efficiente ok? efficiente in termini di tempo ok? questo per alcune ragioni tecniche si chiamerà polinomiale ok? e va bene ma fare questo risolvere questo task cioè ok? cioè l'insieme di tutti i problemi per cui una soluzione è calcolabile in tempo efficiente coincide con tutti quelli per cui invece una soluzione è verificabile
 in tempo efficiente cioè verificabile significa ti do una soluzione e puoi vedere se è corretta in tempo efficiente ok? quindi se puoi vedere che è corretta in tempo efficiente puoi anche trovarla si? sembra una cosa cioè una cosa magica cioè se fosse vero sembra una magia grazie se me la dai riesco a trovare
 a controllarla però diciamo che non la so trovare cioè ok? giusto? eppure non sappiamo mai il posto ok? cioè nel senso che quindi questa è la più grande domanda aperta in informatica teorica P uguale NP o P diverso da NP ok? P diverso da NP significherebbe esiste un problema per cui verificare una soluzione è più facile che trovarne uno ok? dove questo gap tra facile e non facile è quello che diciamo
 quello che passa tra efficiente e non efficiente che però ancora non abbiamo definito i termini precisi ma lo faremo va bene ok? è chiara questa domanda? questo è il più grande problema dell'informatica teorica a cui non sappiamo dare risposta però diciamo la gente pensa che P è diverso da NP ok? ma nessuno lo ha dimostrato e il progresso su questa domanda è bloccato da 50 anni più o meno cioè non è proprio così
 ovviamente so minimizzando però diciamo alcune delle cose che vedremo in questo corso verso la fine di questo corso sono i passi più importanti in avanti per dimostrare che P è diverso da NP però più o meno non la accendiamo per mai non è proprio così più o meno ok?
 altra cosa che non sappiamo è se P è uguale a P space ok? cioè se puoi usare se puoi risolvere un problema in poco spazio ok?
 ma eventualmente tanto tempo ok? cioè quindi vedremo che quando parleremo di complessità di spazio non ci occuperemo del tempo ok? mentre invece il tempo limita lo spazio ok? cioè questa sarà una cosa abbastanza chiara quando studieremo la complessità di spazio cioè è chiaro che se io
 ho un limite di tempo non posso poi usare troppo spazio no? perché ho tempo limitato quindi non posso scrivere all'infinito per esempio ok? invece se ho potrebbe anche essere il contrario che magari se ho più spazio posso farlo in meno tempo perché magari mi salvo più cose se ho più spazio magari ci metto meno tempo e questo in effetti per alcune cose sarà vero
 ok? se puoi risolvere un problema in poco spazio puoi risolvere in poco tempo? poco tempo? si? non sappiamo la risposta P è uguale a P space
 ok? cioè ogni algoritmo efficiente può essere derandomizzato? no non si sa ok? cioè quindi sembra a un certo punto spesso succede come vi avevo già raccontato che
 per risolvere alcuni problemi le prime soluzioni sono randomizzate e sembra inerente il fatto che lo puoi fare soltanto in maniera randomizzata ma poi dopo un po' appare sempre un algoritmo deterministico che fa più o meno la stessa cosa ok? almeno diciamo più o meno in termini di efficienza asintotica ok? e quindi la domanda è ma ogni algoritmo può essere derandomizzato? ok? questo non lo sappiamo ce ne sono tanti altri e
 non avremo tempo diciamo di studiare queste cose quindi non ve lo dirò non ve ne scriverò va bene quindi invece quello che faccio è vi do giusto la prima definizione di complessità di tempo ok? cioè cominciamo a definire la complessità di tempo cioè iniziamo dal tempo iniziamo con il definire la complessità di tempo
 va bene? e sarebbe questo che sia m una tm decisore deterministica ok? quindi cominciamo caso deterministica
 va bene? e quindi m una tm decisore e allora la complessità di tempo di tempo tm è t
 una funzione dai naturali ai naturali tale che che cos'è questa t? t di n per ogni n quindi t di n è uguale al massimo su x in sigma star con x di lunghezza n ok? cioè quindi tra tutti t di n quindi la complessità di tempo di una macchina di Turing è una funzione di n
 ok? per ogni possibile input ok? di quella fissata lunghezza ok? quindi questa è una definizione worst case perché stiamo prendendo tutti gli input prendiamo il massimo running time della macchina di Turing su quel su tutti gli input di quella lunghezza ok? cioè quindi il massimo di che cosa? del numero di passi numero di passi richiesti
 m di x ok? va bene? si? non c'era un grande dove era un grande? è il massimo
 del numero di passi richiesti da m di x poi ovviamente per una particolare m potrei non saperlo contare in maniera esatta e potrei quindi dare un upper bottom si? ok? cioè però diciamo la definizione di complessità di tempo non c'ha dentro log grade non c'ha dentro niente è una cosa è precisamente una funzione che ti dice qual è il massimo per ogni n ti dice qual è il massimo numero di passi della macchina di Turing su tutti gli input di lunghezza
 m si? ok? quindi diciamo qui siamo interessati no? siamo interessati siamo interessati a come questa funzione si comporta come questa funzione a come questa funzione a come questa funzione
 si comporta in funzione no? in termini della lunghezza dell'in ok?
 va bene? quindi vedremo no? cioè vedremo che ad esempio cioè questo il concetto di il concetto di complessità di tempo ci porterà a definire infatti i primi esempi di classi di complessità per computazioni deterministiche ok? e queste saranno per esempio P dove vuol dire che vuol dire P vuol dire che questa complessità è
 limitata da un polinomio nella lunghezza m ok? e lì dentro a quella scatola ci saranno quindi tutte le macchine di Turing no? tutti i problemi risolvibili da macchine di Turing con complessità di tempo polinomiale ok? questa scelta di polinomiale diciamo come vedremo sarà motivata dal fatto che tante delle cose che tante delle cioè una cosa importante no? sarà che diciamo
 cioè come vedremo come vedremo quando cominciamo adesso a parlare di complessità di tempo se vi ricordate no? abbiamo studiato tanti diversi modelli di computazione no? cioè nel senso abbiamo detto la macchina di Turing singolo nastro eh illimitato a destra oppure la macchina di Turing che può fare avanti destra sinistra e stare ferma oppure la macchina di Turing che ha K nastri ok? abbiamo detto queste sono tutte equivalenti questo l'abbiamo detto prima di mettere in mente
 il mezzo tempo spazio no? si come vedremo invece adesso cambiano le cose ok? cioè se considero una macchina di Turing a due nastri questa potrebbe fare cose che una macchina di Turing ha un nastro non fa nello stesso tempo ok? cioè nello stesso tempo ok? perché ovviamente possono fare le stesse cose se non c'è stato il tempo ok? quindi questo sarà importante però seppure questo è vero per numeri concreti
 per numeri costanti e quindi ci può essere un fattore di scala costante quando si comincia a considerare invece le classi di complessità cioè tipo infatti tempo polinomiale tutte queste differenze verranno mangiate saranno tutte caritate tutte nella stessa classe cioè quindi questo è importante no? perché il nostro concetto di efficienza ok? efficienza asintotica deve essere invariante rispetto al fatto che c'è una macchina di Turing a un nastro a dieci nastri a cento nastri
 va bene? quindi questo sarà evidente a partire da ora ci possiamo fermare per oggi
